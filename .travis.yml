language: node_js

node_js:
  - 10

branches:
  only:
    - sources

before_script:
  - npm install

script:
  - gatsby build

deploy:
  provider: pages
  local-dir: public
  target-branch: master
  github-token: $GITHUB_TOKEN
  keep-history: true
  skip-cleanup: true
  on:
    branch: sources
